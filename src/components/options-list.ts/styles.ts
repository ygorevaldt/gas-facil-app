import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    marginTop: 32,
    gap: 8,
  },
  optionsLabel: {
    fontSize: 16,
    fontWeight: "bold",
    marginBottom: 8,
    textAlign: "left",
  },
  optionsList: {
    flexDirection: "row",
    gap: 8,
    width: "100%",
  },
});
